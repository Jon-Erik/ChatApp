{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "format": "prettier --write \"*.js\" \"*.tag\"",
    "build": "rm -rf dist && set NODE_ENV=production && parcel build index.html",
    "watch": "parcel watch index.html"
  },
  "author": "Jon-Erik Chandler <jchandler@icomputeconsulting.com>",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.11.0",
    "parcel-plugin-riot": "^2.0.0",
    "prettier": "^1.16.4"
  },
  "dependencies": {
    "express": "^4.16.4",
    "riot": "^3.13.2"
  }
}
